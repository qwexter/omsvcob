[versions]
junit = "5.10.0"

[libraries]
benchmark = { module = "org.jetbrains.kotlinx:kotlinx-benchmark-runtime", version = "0.4.13" }
junit-bom = { module = "org.junit:junit-bom", version.ref = "junit" }
junit-jupiter = { module = "org.junit.jupiter:junit-jupiter", version.ref = "junit" }

[plugins]
benchmark = { id = "org.jetbrains.kotlinx.benchmark", version = "0.4.13" }
kotlin = { id = "org.jetbrains.kotlin.jvm", version = "2.0.20" }